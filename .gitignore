# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/

# Environment Variables (NEVER commit these!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
tsconfig.tsbuildinfo

# Vercel
.vercel

# IDE
.vscode/
.idea/
*.swp
*.swo

# Python (handoff/pre_tick.py)
__pycache__/
*.pyc

# Misc
.eslintcache

# Hermes agent working directory (local tooling state, not project source)
.hermes/

# Throwaway one-off scripts used to poke the live database during debugging.
# They read credentials out of .env.local at runtime and several use the
# service-role key, so they must never be committed even though the secrets
# themselves live outside the file.
tmp-*.mjs
